{
  "name": "theophysics-semantic-tagger-v2",
  "version": "1.0.0",
  "description": "Streaming semantic tagging plugin for Obsidian.",
  "main": "main.js",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --external:obsidian --outfile=main.js --target=es2018 --format=cjs --platform=node"
  },
  "dependencies": {
    "obsidian": "latest"
  },
  "devDependencies": {
    "@types/node": "^16.0.0",
    "esbuild": "^0.17.0",
    "typescript": "^4.0.0"
  }
}
